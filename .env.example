# TLS cert paths for the negotiateTLS integration test.
# Copy to .env and set to your cert paths; the test is skipped when unset.
SMTP_CHANNEL_TLS_CERT=/path/to/your.crt
SMTP_CHANNEL_TLS_KEY=/path/to/your.key
